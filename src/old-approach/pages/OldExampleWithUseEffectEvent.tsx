import React from 'react';
import ShoppingCartTracker from '../components/ShoppingCartTracker';

const New: React.FC = () => {
    return (
        <div>
            <h2>React 18</h2>
            <ShoppingCartTracker />
        </div>
    );
};

export default New;
